{
  "name": "@airgram/vue",
  "version": "0.1.0",
  "private": false,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "keywords": [
    "telegram",
    "tdweb",
    "airgram",
    "vue"
  ],
  "scripts": {
    "build": "gulp build",
    "check": "tsc --noEmit",
    "lint": "eslint --fix ./src/**/*.ts",
    "prepublishOnly": "gulp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pre-commit": "yarn run lint",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@airgram/core": "link:../../packages/core",
    "vue": "^2.6.10"
  },
  "devDependencies": {
    "@airgram/use-models": "link:../../packages/use-models",
    "@types/tdweb": "1.4.0"
  },
  "peerDependencies": {
    "@airgram/web": "link:../../packages/web"
  }
}
